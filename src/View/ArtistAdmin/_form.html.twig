
<form method="post">
    <input type="hidden" value="{{ artist.id }}" name="id">
    <input type="text" value="{{ artist.title }}" name="title" placeholder="Artist">
    <input type="text" value="{{ artist.local }}" name="local" placeholder="Local">
    <input type="submit" value="Valider">
</form>